# --- Broker / market data ---
smartapi-python==1.5.5      # Angel One SmartAPI SDK (SmartConnect, SmartWebSocketV2)
pyotp>=2.9.0                # TOTP generation for SmartAPI login

# --- Core ---
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
cryptography>=42.0          # Fernet credential encryption
pandas>=2.2                 # scrip master parsing / strike selection
requests>=2.31              # scrip master download

# --- Dashboard (Phase 2) ---
streamlit>=1.37             # read-only live dashboard; needs st.fragment(run_every=...)

# --- Alerts (Phase 4) ---
# requests (already above) powers the lightweight Telegram push channel.
# python-telegram-bot is only needed for the interactive command bot:
python-telegram-bot>=21     # alerts/telegram_bot.py (optional; comment out if unused)

# --- Dev / tests ---
pytest>=8.0
